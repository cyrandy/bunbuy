<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.markupartist.android.widget.actionbar"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:icon="@drawable/logo_pic"
        android:label="@string/app_name" 
        android:theme="@android:style/Theme.NoTitleBar">
        <activity
            android:name=".CheckRequest"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".SendRequest"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".CheckCreatedActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BunbuyActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".WhatForDinner"></activity>
        <activity android:name=".CreateTrend"></activity>
        <activity android:name=".CreateEvent"></activity>
    </application>

</manifest>